 /**:
  ros__parameters:
    # Filter dynamics parameters
    zeta: [1.0, 1.0]
    omega: [1.0, 1.0]

    # Topics
    topics:
      guidance_gripper: "vortex/gripper/guidance"
      gripper_state: "vortex/gripper/state"
      
    # Action servers
    action_servers:
      gripper_reference_filter: "/nautilus/gripper/reference_filter"
